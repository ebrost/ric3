$(function() {$('#tabs a').click(function (e) {
    
        e.preventDefault();
        console.log('click');
        $(this).tab('show');
    });
    
 // store the currently selected tab in the hash value
    
    })
